# BALANCE — Bush Rush

Матрица 12×12 шаблонов, бот против бота, `SEEDS=20 tools/build.sh autoplay` → `unity/CI/autoplay.txt` (копии в `balance/`). Правила — GDD.md.

## 1. Первый прогон (14.09) — заполняется после первой сборки
